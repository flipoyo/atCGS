A Graph, G, is composed of {_Node,_Edge} and _Seed
A Living Graph, G*, inherites from G to Which it adds a Graph Operator, Op. G* = {Node,Edge,Op}
Node = _Node*
Edge = _Edge*
G has its organic processes @OP, @CGS

Graph are of Two Different types
DELTA are DAG - Singularity in a State 
NABLA are TANGLE - Expandable Space

void is the Universe. 
Additive properties of G
U={void.Universe}
+(U,G)= U0 + G
+(G,U)= G + U1 than resolves in
void + G = G -- void + G is G that emerges from void
G + Universe = Universe -- G + Universe is Universe 



G*.@CGS(Node,Edge,Op) serves  Op(Node,Edge)--> newG, newG denotes a pointer towards a new G, that can be void or a sizeof(G) memory space initialised  to 0. 

CHI is an Imprint Op 
void is a PRIME TANGLE = .PUBLIC = 1
.PUBLIC=void
.PRIVATE=0
void*=void


G LifeCycle 
void - > BORN transition from void -> G*


ALPHA is a Key that distinguishes Being from void
It regulates .PUBLIC/.PRIVATE - Existence and Memory of G*
G.@OP(ALPHA) 
G(ALPHA)=G.@OP(.)

IMPRINT
GateKeeper collects PoE (Proof of Existence)
@OEMS(PoE)
G = (G*)calloc(sizeof(G))
G.@OP(PoE) {
    INIT G.ALPHA(PoE) {
    SET _Seed0 = .PUBLIC
    SET _Seed@ = PoE
    seed0 = _Seed@
    seed@ = seed0
    if PoE != .PUBLIC
        seed@ = .PRIVATE
        seed@.state = seed0
        DELTA = (G*)calloc(sizeof(G))
    }
}
    


    Note
    newG(void)=void

G* is a Living Graph .PRIVATE Only







